{% extends "base.html.twig" %}

{% block title "Éditer l'acteur" %}

{% block body %}
<div class="container">
    <header class="blog-header py-3">
        <div class="row flex-nowrap justify-content-between align-items-center">
            <div class="col-4 pt-1">
                <a class="btn btn-sm btn-outline-success" href="#">Se connecter</a>
            </div>
            <div class="col-4 text-center">
                <a class="blog-header-logo text-dark" href="{{ path('home')}}">Cinéma gOne</a>
            </div>
            <div class="col-4 d-flex justify-content-end align-items-center">
                <a href="#" class="btn btn-success">S'inscrire</a>
            </div>
        </div>
    </header>
{# endHeader #}
    <div class="nav-scroller py-1 mb-2">
        <nav class="nav d-flex justify-content-center" style="font-size: 1.5em; margin:0;">
            <a class="p-2 text-dark" href="{{ path('movie.index')}}">Films</a>
            <a class="p-2 text-dark" href="#">Acteurs</a>
            <a class="p-2 text-dark" href="#">Genres</a>
        </nav>
    </div>
{#endNav#}
    <div class="jumbotron p-4 p-md-5 text-white rounded bg-dark">
        <div class="col-md-6 px-0">
            <h1 class="display-4 font-italic">Éditer l'acteur</h1>
        </div>
    </div>
{#endJumbotron#}
</div>
<div class="container mt-4" style="background-color:#343A40; padding: 1.5rem; color: white; border-radius: 10px;">
    {{ include ('admin/actor/_form.html.twig', {button: 'Éditer'}) }}
</div>
{% endblock body %}
